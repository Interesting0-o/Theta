# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# Environment secrets (真实密钥，勿提交)
.env
.env.*
!.env.example

#agent
.claude
CLAUDE.md

#ide
.vscode
.idea/
.nvim

#资源（**锚定项目根**：裸 `resource` 会连 `app/resource/` 这个源码包一起吞掉——2026-09-21 踩到）
/resource

# LangGraph dev 服务器运行产物
.langgraph_api/

# 默认沙箱工作区（未指定 WORKSPACE_PATH 时模型操作目录；同样锚定根，避免吞掉别处的 tmp/）
/tmp/